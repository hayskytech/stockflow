{
  "name": "stockflow",
  "private": true,
  "version": "1.0.0",
  "description": "StockFlow — root dev orchestration for backend + frontend",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
